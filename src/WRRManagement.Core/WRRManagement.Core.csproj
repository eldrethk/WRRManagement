<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Allow the back-office admin app to access internal setters on entities -->
    <InternalsVisibleTo Include="WRR.Admin" />
  </ItemGroup>

</Project>
